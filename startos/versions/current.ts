import { VersionInfo } from '@start9labs/start-sdk'

export const current = VersionInfo.of({
  version: '0.23.1-rc.0:9',
  releaseNotes: {
    en_US: `Advances the bundled vLLM nightly build to commit 4559c43 (still on the upstream 0.23.1rc0 line). Highlights: new Unlimited OCR and openai/privacy-filter model support, while the legacy Grok and Aquila architectures were removed; the CPU build gains compressed-tensor w8a8 int8 MoE, DFlash speculative decoding, and a macOS/Apple-Silicon build-hang fix (OpenMP re-enabled); the Rust frontend adds a Harmony renderer for GPT-OSS, switches TLS to native-tls/OpenSSL, and preserves tool-parser output ordering; and \`vllm chat\` gains a flag to print TTFT and TPS. Bugfixes include corrected reasoning-end detection on prompt history, rejecting min_tokens above max_tokens, and relative local media paths. Full changes: https://github.com/vllm-project/vllm/compare/3f5a1e1...4559c43`,
    es_ES: `Avanza la compilación nightly de vLLM incluida al commit 4559c43 (sigue en la línea upstream 0.23.1rc0). Destacados: nuevo soporte de modelos Unlimited OCR y openai/privacy-filter, mientras que se eliminaron las arquitecturas heredadas Grok y Aquila; la compilación de CPU gana MoE int8 w8a8 de compressed-tensor, decodificación especulativa DFlash y una corrección del bloqueo de compilación en macOS/Apple Silicon (OpenMP reactivado); el frontend Rust añade un renderizador Harmony para GPT-OSS, cambia TLS a native-tls/OpenSSL y conserva el orden de salida del analizador de herramientas; y \`vllm chat\` gana una opción para imprimir TTFT y TPS. Las correcciones incluyen la detección corregida del fin de razonamiento en el historial de prompts, el rechazo de min_tokens por encima de max_tokens y rutas relativas de medios locales. Cambios completos: https://github.com/vllm-project/vllm/compare/3f5a1e1...4559c43`,
    de_DE: `Aktualisiert den gebündelten vLLM-Nightly-Build auf Commit 4559c43 (weiterhin auf der Upstream-Linie 0.23.1rc0). Highlights: neue Modellunterstützung für Unlimited OCR und openai/privacy-filter, während die veralteten Architekturen Grok und Aquila entfernt wurden; der CPU-Build erhält Compressed-Tensor-w8a8-int8-MoE, DFlash-Speculative-Decoding und eine Behebung des Build-Hangs unter macOS/Apple Silicon (OpenMP wieder aktiviert); das Rust-Frontend ergänzt einen Harmony-Renderer für GPT-OSS, stellt TLS auf native-tls/OpenSSL um und bewahrt die Ausgabereihenfolge des Tool-Parsers; und \`vllm chat\` erhält eine Option zur Ausgabe von TTFT und TPS. Fehlerbehebungen umfassen die korrigierte Reasoning-Ende-Erkennung im Prompt-Verlauf, die Ablehnung von min_tokens über max_tokens und relative lokale Medienpfade. Vollständige Änderungen: https://github.com/vllm-project/vllm/compare/3f5a1e1...4559c43`,
    pl_PL: `Aktualizuje dołączoną kompilację nightly vLLM do commitu 4559c43 (nadal na upstreamowej linii 0.23.1rc0). Najważniejsze: nowa obsługa modeli Unlimited OCR i openai/privacy-filter, przy jednoczesnym usunięciu przestarzałych architektur Grok i Aquila; kompilacja CPU zyskuje MoE int8 w8a8 compressed-tensor, dekodowanie spekulatywne DFlash oraz poprawkę zawieszania kompilacji na macOS/Apple Silicon (ponownie włączony OpenMP); frontend Rust dodaje renderer Harmony dla GPT-OSS, przełącza TLS na native-tls/OpenSSL i zachowuje kolejność wyjścia parsera narzędzi; a \`vllm chat\` zyskuje flagę do wypisywania TTFT i TPS. Poprawki obejmują skorygowane wykrywanie końca rozumowania w historii promptów, odrzucanie min_tokens powyżej max_tokens oraz względne ścieżki mediów lokalnych. Pełne zmiany: https://github.com/vllm-project/vllm/compare/3f5a1e1...4559c43`,
    fr_FR: `Met à jour la version nightly de vLLM incluse vers le commit 4559c43 (toujours sur la ligne upstream 0.23.1rc0). Points forts : nouvelle prise en charge des modèles Unlimited OCR et openai/privacy-filter, tandis que les architectures héritées Grok et Aquila ont été supprimées ; la build CPU gagne le MoE int8 w8a8 compressed-tensor, le décodage spéculatif DFlash et une correction du blocage de build sur macOS/Apple Silicon (OpenMP réactivé) ; le frontend Rust ajoute un moteur de rendu Harmony pour GPT-OSS, bascule TLS vers native-tls/OpenSSL et préserve l'ordre de sortie du parseur d'outils ; et \`vllm chat\` gagne une option pour afficher TTFT et TPS. Les corrections incluent la détection corrigée de fin de raisonnement dans l'historique des prompts, le rejet de min_tokens supérieur à max_tokens et les chemins relatifs de médias locaux. Changements complets : https://github.com/vllm-project/vllm/compare/3f5a1e1...4559c43`,
  },
  migrations: {
    up: async ({ effects }) => {},
    down: async ({ effects }) => {},
  },
})
