import { VersionInfo } from '@start9labs/start-sdk'

export const current = VersionInfo.of({
  version: '0.22.1-rc.0:9',
  releaseNotes: {
    en_US: `Advances the bundled vLLM nightly build to commit 54bbf51 (still upstream 0.22.1rc0). Highlights: fixes a crash in the nightly Docker images (ImportError: AnthropicOutputConfig), two security fixes (DoS via prompt_embeds on M-RoPE models and a regex-compilation timeout guard for structured outputs), an SM90 cutlass fp8 odd-M kernel perf improvement (NVIDIA), a CPU source-build uv dependency-resolution fix (relevant to the cpu variant), and new /derender endpoints for disaggregated postprocessing. Full changes: https://github.com/vllm-project/vllm/compare/1033ffa...54bbf51`,
    es_ES: `Avanza la compilación nightly de vLLM incluida al commit 54bbf51 (sigue siendo upstream 0.22.1rc0). Novedades: corrige un fallo en las imágenes Docker nightly (ImportError: AnthropicOutputConfig), dos correcciones de seguridad (DoS mediante prompt_embeds en modelos M-RoPE y un límite de tiempo para la compilación de regex en salidas estructuradas), una mejora de rendimiento del kernel fp8 cutlass SM90 con M impar (NVIDIA), una corrección de resolución de dependencias uv en la compilación desde fuente en CPU (relevante para la variante cpu) y nuevos endpoints /derender para el posprocesamiento desagregado. Cambios completos: https://github.com/vllm-project/vllm/compare/1033ffa...54bbf51`,
    de_DE: `Aktualisiert den gebündelten vLLM-Nightly-Build auf Commit 54bbf51 (weiterhin Upstream 0.22.1rc0). Highlights: behebt einen Absturz in den Nightly-Docker-Images (ImportError: AnthropicOutputConfig), zwei Sicherheitsfixes (DoS über prompt_embeds bei M-RoPE-Modellen und ein Timeout-Schutz für die Regex-Kompilierung bei strukturierten Ausgaben), eine Leistungsverbesserung des SM90-cutlass-fp8-Kernels bei ungeradem M (NVIDIA), eine Korrektur der uv-Abhängigkeitsauflösung beim CPU-Quellbuild (relevant für die cpu-Variante) und neue /derender-Endpunkte für die disaggregierte Nachbearbeitung. Vollständige Änderungen: https://github.com/vllm-project/vllm/compare/1033ffa...54bbf51`,
    pl_PL: `Aktualizuje dołączoną kompilację nightly vLLM do commitu 54bbf51 (nadal upstream 0.22.1rc0). Najważniejsze: naprawia awarię w obrazach Docker nightly (ImportError: AnthropicOutputConfig), dwie poprawki bezpieczeństwa (DoS przez prompt_embeds w modelach M-RoPE oraz limit czasu kompilacji regex w uporządkowanych wyjściach), poprawę wydajności jądra fp8 cutlass SM90 dla nieparzystego M (NVIDIA), poprawkę rozwiązywania zależności uv w kompilacji ze źródeł na CPU (istotne dla wariantu cpu) oraz nowe endpointy /derender do zdezagregowanego przetwarzania końcowego. Pełne zmiany: https://github.com/vllm-project/vllm/compare/1033ffa...54bbf51`,
    fr_FR: `Met à jour la version nightly de vLLM incluse vers le commit 54bbf51 (toujours upstream 0.22.1rc0). Points forts : corrige un plantage dans les images Docker nightly (ImportError: AnthropicOutputConfig), deux correctifs de sécurité (DoS via prompt_embeds sur les modèles M-RoPE et un garde-fou de délai pour la compilation des regex dans les sorties structurées), une amélioration des performances du noyau fp8 cutlass SM90 pour M impair (NVIDIA), un correctif de résolution des dépendances uv lors de la compilation depuis les sources sur CPU (pertinent pour la variante cpu) et de nouveaux endpoints /derender pour le post-traitement désagrégé. Changements complets : https://github.com/vllm-project/vllm/compare/1033ffa...54bbf51`,
  },
  migrations: {
    up: async ({ effects }) => {},
    down: async ({ effects }) => {},
  },
})
